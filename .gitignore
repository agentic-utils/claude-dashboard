# Runtime diagnostics — written next to the script, local-only, never commit.
claude-dashboard.log
*.log

# Adversarial-review scratch
.review/

# Local flag defaults, machine-specific — never commit.
.claude-dashboard.rc

# Python
__pycache__/
*.pyc
